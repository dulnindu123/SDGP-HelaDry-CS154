name: flutter_application_1
description: "HelaDry - Solar-Powered Food Dehydration App"
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ^3.10.7

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8
  provider: ^6.1.2
  shared_preferences: ^2.3.4
  google_fonts: ^6.2.1
  firebase_core: ^4.5.0
  firebase_auth: ^6.2.0
  http: ^1.6.0
  firebase_database: ^12.1.4

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
  assets:
    - assets/images/
